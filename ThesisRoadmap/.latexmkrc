# This document has no \bibliography, so bibtex is disabled to keep the build
# clean: latexmk otherwise runs bibtex and errors when there is no bibliography.
# If you add a \bibliography block with citations, delete this file so they
# resolve and render.
$bibtex_use = 0;
